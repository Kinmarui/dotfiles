# downloaded plugin binaries (fetched at install time)
config/zellij/plugins/*.wasm

# backups bootstrap leaves behind
*.bak
*.bak.*

# local secrets (never commit — this repo is public)
secrets

# private overlay shell fragment (identity/server maps live in dotfiles-private)
config/shell/private.sh
